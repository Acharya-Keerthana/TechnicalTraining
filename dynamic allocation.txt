// Online C compiler to run C program online
#include <stdio.h>
#include <stdlib.h>

int main() {
    // Write C code here
    int a[5],i;
    printf("Enter the static array element:");
    for(i=0;i<5;i++){
        scanf("%d",&a[i]);
    }
   printf("static array element:");
    for(i=0;i<5;i++){
        printf("%d ",a[i]);
    }
    int n1;
    printf("\n enter the number of elements for the dynamic array:");
    scanf("%d",&n1);
    int *b=(int*)malloc(n1*sizeof(int));
    if(b==NULL){
        printf("Memory allocation failed\n");
        return 0;
    }
    printf("\nEnter %d elements for the dynamic array:\n",n1);
    for(i=0;i<n1;i++){
        scanf("%d",&b[i]);
    }
    printf("elements form the dynamic array:\n");
    for(i=0;i<n1;i++){
        printf("%d ",b[i]);
    }
    int n2;
    printf("\n enter the new number of elements for the dynamic array:");
    scanf("%d",&n2);
    b=(int*)realloc(b,n2*sizeof(int));
    if(n2>n1){
    printf("Enter %d elements for the dynamic array:\n",n2);
    for(i=n1;i<n2;i++){
        scanf("%d",&b[i]);
    }
    }
    printf("Resized array elemetns:\n");
    for(i=0;i<n2;i++){
        printf("%d ",b[i]);
    }
    
   

    return 0;
}